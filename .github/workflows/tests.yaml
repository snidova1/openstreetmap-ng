name: Run tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - "*"

jobs:
  test:
    name: ${{ matrix.implementation }} tests (${{ matrix.os }})
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        implementation: [python, cython]
        exclude:
          - os: macos-latest
            implementation: cython
      fail-fast: false

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup

      - name: Build Cython modules
        if: matrix.implementation == 'cython'
        run: |
          nix-shell --pure --run "cython-build-fast"

      - name: Start services
        run: |
          nix-shell --pure --run "dev-start"

      - name: Run tests
        env:
          PYTHONFAULTHANDLER: 1
        run: |
          nix-shell --pure --run "python -X faulthandler -m pytest --verbose --no-header --randomly-seed=1778880973 --extended || true"
